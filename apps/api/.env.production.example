# Production Environment Variables for Workers API
# Set these using: wrangler secret put <KEY_NAME>

# DO NOT set these in .env files - use wrangler secret put instead!
# These are for reference only.

# JWT Authentication
# Generate with: openssl rand -base64 32
# Must match JWT_SECRET in the frontend
JWT_SECRET=your_jwt_secret_here

# Internal API Key (for recorder server)
# Generate with: openssl rand -base64 32
INTERNAL_API_KEY=your_secure_random_api_key_here
